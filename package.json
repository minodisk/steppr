{
  "name": "steppr",
  "version": "0.0.2",
  "description": "Terminal spinner for step execution",
  "main": "lib/index.js",
  "author": "Daisuke Mino",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf lib/ && yarn flow-remove-types -d lib/ src/ && yarn format",
    "test": "yarn jest",
    "format": "yarn prettier --write src/*.js lib/*.js",
    "prepublish": "yarn build"
  },
  "jest": {
    "transform": {
      "\\.js$": "./jest/flow-remove-types"
    }
  },
  "dependencies": {
    "chalk": "^2.3.0",
    "cli-spinners": "^1.1.0",
    "log-update": "^2.3.0"
  },
  "devDependencies": {
    "flow-bin": "^0.59.0",
    "flow-remove-types": "^1.2.3",
    "jest": "^22.1.3",
    "prettier": "^1.9.2"
  }
}
